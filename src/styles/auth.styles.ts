import { styles as all } from './styles';

export const authStyles = {
  authContainer: all.authContainer,
  authTitle: all.authTitle,
  authSubTitle: all.authSubTitle,
  authInput: all.authInput,
  authButton: all.authButton,
  authButtonText: all.authButtonText,
  center: all.center,
  placeholderTabText: all.placeholderTabText,
};
